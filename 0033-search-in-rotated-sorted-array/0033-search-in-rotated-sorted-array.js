/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var search = function(nums, target) {
    /*
            
        
        
    */
    return nums.indexOf(target) >= 0 ? nums.indexOf(target) : -1
};